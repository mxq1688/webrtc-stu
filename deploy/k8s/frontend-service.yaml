apiVersion: v1
kind: Service
metadata:
  name: webrtc-frontend
  namespace: xiaozhi
  labels:
    app: webrtc-frontend
spec:
  type: NodePort
  selector:
    app: webrtc-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30810
